# 概要
通常のテトリスとは異なるが、ミノを置くエリアを縦横ともに2倍（20列 × 40行）に拡大する。

# タスク
- [ ] `script.js` の `ROWS` を 40、`COLS` を 20 に変更
- [ ] 画面に収めるため `BLOCK_SIZE` を 15 に変更
- [ ] `style.css` のグリッド背景（`background-size`）を 15px に変更
