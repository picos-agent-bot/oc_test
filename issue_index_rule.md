# 概要
新規にサブフォルダ（アプリケーション）を追加した際は、必ずルートの `index.html` にリンクを追記する。
今回は `/tetris` のリンクを追記し、さらにこの運用ルールを `README.md` に記載する。

# タスク
- [ ] ルートの `index.html` に `/tetris` へのリンクを追加
- [ ] `README.md` の開発フローに、「サブフォルダ追加時は必ずルートのindex.htmlにリンクを追加する」というルールを追記
